{
  "name": "project-seger-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "node src/server.js",
    "migrate": "prisma migrate dev --name init",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@prisma/client": "^4.0.0",
    "dotenv": "^16.0.0",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "prisma": "^4.0.0"
  }
}
